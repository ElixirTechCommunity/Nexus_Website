version: "3.8"
services:
  nexus_website:
    build:
      dockerfile: dockerfile
      context: .
    ports:
      - "80:80"
    volumes:
      - /app/node_modules
      - .:/app/
    container_name: nexus
